"use client";

export default function Dashboard({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  // const id = (await params).id;

  return (
    <>
      link page - <p></p>
    </>
  );
}
